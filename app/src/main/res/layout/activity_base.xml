<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/baseLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/darkTransparent"
    android:foregroundTint="@color/grey"
    android:visibility="visible"
    tools:context=".activities.BaseActivity">

    <ProgressBar
        android:id="@+id/baseProgressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="250dp"
        android:layout_height="250dp"
        android:indeterminateTint="@color/grey"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/baseProgressBarIco"
        android:layout_width="90dp"
        android:layout_height="90dp"
        app:layout_constraintBottom_toBottomOf="@+id/baseProgressBar"
        app:layout_constraintEnd_toEndOf="@+id/baseProgressBar"
        app:layout_constraintStart_toStartOf="@+id/baseProgressBar"
        app:layout_constraintTop_toTopOf="@+id/baseProgressBar"
        app:srcCompat="@drawable/ico_light" />
</androidx.constraintlayout.widget.ConstraintLayout>